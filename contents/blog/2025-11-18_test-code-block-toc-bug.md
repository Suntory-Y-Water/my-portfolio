---
title: 目次のコードブロック検出テスト
slug: test-code-block-toc-bug
date: 2025-11-18
description: コードブロック内の見出しが目次に含まれないことを確認するためのテスト記事です。
icon: 🧪
icon_url: https://raw.githubusercontent.com/microsoft/fluentui-emoji/main/assets/Test%20tube/Flat/test_tube_flat.svg
tags:
  - テスト
---

## 実際の見出し1

これは実際の見出しです。目次に含まれるべきです。

### 実際のサブ見出し1-1

これも実際の見出しです。目次に含まれるべきです。

## コードブロックのテスト

以下のコードブロック内の見出しは目次に含まれるべきではありません。

```md
## コードブロック内の見出し1
これは目次に含まれてはいけません。

### コードブロック内のサブ見出し1
これも目次に含まれてはいけません。
```

```yaml
## YAMLコメントとしての見出し
name: テスト
on: push
```

```typescript
// ## TypeScriptコメント内の見出し
function test() {
  console.log("## これは文字列内の見出し");
}
```

## 実際の見出し2

これは実際の見出しです。目次に含まれるべきです。

以下はマークダウンコードブロックの例です：

```markdown
## マークダウン例の見出し

これは目次に含まれてはいけません。

### マークダウン例のサブ見出し

これも目次に含まれてはいけません。
```

### 実際のサブ見出し2-1

最後の実際の見出しです。目次に含まれるべきです。

## まとめ

このテスト記事では、以下の見出しのみが目次に含まれるべきです：

1. 実際の見出し1
   1. 実際のサブ見出し1-1
2. コードブロックのテスト
3. 実際の見出し2
   1. 実際のサブ見出し2-1
4. まとめ

コードブロック内の見出しは含まれてはいけません。
