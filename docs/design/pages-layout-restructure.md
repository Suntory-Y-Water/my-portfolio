# ページレイアウト構造変更仕様書

## 概要

トップページとブログページの構成を変更する。

**変更内容:**
- 現在のトップページ（`/`） → `/about` に移動
- 現在のブログページ（`/blog`） → トップページ（`/`）に移動

## 現状の構成

### ルーティング構造

| パス | ファイル | 内容 | タイトル |
|------|---------|------|---------|
| `/` | `src/app/page.tsx` | 自己紹介ページ | 「私について」 |
| `/blog` | `src/app/blog/page.tsx` | ブログ記事一覧（最新5件） | なし |
| `/blog/page/[page]` | `src/app/blog/page/[page]/page.tsx` | ブログページネーション | - |
| `/blog/[slug]` | `src/app/blog/[slug]/page.tsx` | ブログ記事詳細 | - |
| `/posts` | `src/app/posts/page.tsx` | 外部記事一覧（Qiita、Zenn、note） | 「記事一覧🖊️」 |
| `/tags` | `src/app/tags/page.tsx` | タグ一覧 | - |

### ヘッダーナビゲーション

`src/components/shared/Header.tsx` の `NAVIGATION_LINKS`：
```typescript
[
  { href: '/', title: 'Home', icon: <IoMdHome /> },
  { href: '/posts', title: 'Posts', icon: <MdOutlineArticle /> },
  { href: '/blog', title: 'Blog', icon: <MdOutlineBook /> },
  { href: '/tags', title: 'Tags', icon: <Icons.tag /> }
]
```

### コンポーネント依存関係

**トップページ（現在の `/`）:**
- `Career` コンポーネント（`@/components/feature/about/Career`）
- 自己紹介、経歴、SNSリンク、連絡先を表示

**ブログページ（現在の `/blog`）:**
- `BlogCard` コンポーネント（`@/components/feature/content/blog-card`）
- `getAllBlogPosts()` - マークダウンファイルからブログ記事取得
- `paginateItems()` - ページネーション処理

## 変更後の構成

### ルーティング構造（変更後）

| パス | ファイル | 内容 | 変更 |
|------|---------|------|------|
| `/` | `src/app/page.tsx` | **ブログ記事一覧（最新5件）** | ✏️ 内容変更 |
| `/about` | `src/app/about/page.tsx` | **自己紹介ページ** | ✨ 新規作成 |
| `/blog` | - | - | ❌ 削除（リダイレクト） |
| `/blog/page/[page]` | `src/app/page/[page]/page.tsx` | ブログページネーション | 📁 移動 |
| `/blog/[slug]` | `src/app/[slug]/page.tsx` | ブログ記事詳細 | 📁 移動 |
| `/posts` | `src/app/posts/page.tsx` | 外部記事一覧 | 変更なし |
| `/tags` | `src/app/tags/page.tsx` | タグ一覧 | 変更なし |

### ヘッダーナビゲーション（変更後）

`src/components/shared/Header.tsx` の `NAVIGATION_LINKS`：
```typescript
[
  { href: '/', title: 'Home', icon: <IoMdHome /> },        // トップ → ブログ一覧
  { href: '/about', title: 'About', icon: <...> },        // 新規追加
  { href: '/posts', title: 'Posts', icon: <MdOutlineArticle /> },
  { href: '/tags', title: 'Tags', icon: <Icons.tag /> }
]
```

**変更点:**
- `Blog` リンクを削除
- `About` リンクを追加（適切なアイコン選定が必要）

## 実装の詳細

### 1. 新規作成するファイル

#### `src/app/about/page.tsx`
- 現在の `src/app/page.tsx` の内容をコピー
- タイトル「私について」を維持
- `Career` コンポーネントをそのまま使用

### 2. 移動するファイル

#### `src/app/blog/page.tsx` → `src/app/page.tsx`
- ブログ一覧の内容で上書き
- `getAllBlogPosts()` と `paginateItems()` をそのまま使用
- `BlogCard` コンポーネントをそのまま使用
- 「すべての投稿を見る」リンクを `/blog/page/1` → `/page/1` に変更

#### `src/app/blog/page/[page]/page.tsx` → `src/app/page/[page]/page.tsx`
- ファイルを移動
- 内部のリンク参照を更新（`/blog/` → `/`）

#### `src/app/blog/[slug]/page.tsx` → `src/app/[slug]/page.tsx`
- ファイルを移動
- 内部のリンク参照を更新
- メタデータ内のURL更新（`/blog/${slug}` → `/${slug}`）

#### `src/app/blog/ogp/[slug]/route.tsx` → `src/app/ogp/[slug]/route.tsx`
- OGP画像生成ルートを移動
- 内部の参照を更新

#### `src/app/blog/md/[slug]/route.ts` → `src/app/md/[slug]/route.ts`
- マークダウン取得APIルートを移動
- 内部の参照を更新

### 3. 削除するディレクトリ

- `src/app/blog/` ディレクトリ全体を削除（移動後）
- 移動対象: `page.tsx`, `[slug]/`, `page/[page]/`, `ogp/[slug]/`, `md/[slug]/`, `layout.tsx`

### 4. リダイレクト設定

`next.config.mjs` に以下の設定を追加：

**A. redirects設定の追加:**
```javascript
async redirects() {
  return [
    {
      source: '/blog',
      destination: '/',
      permanent: true, // 301リダイレクト
    },
    {
      source: '/blog/page/:page',
      destination: '/page/:page',
      permanent: true,
    },
    {
      source: '/blog/:slug',
      destination: '/:slug',
      permanent: true,
    },
  ];
}
```

**B. rewrites設定の更新:**
```javascript
// 既存の設定を更新
async rewrites() {
  return [
    {
      source: '/:slug.md',  // /blog/:slug.md → /:slug.md に変更
      destination: '/md/:slug',  // /blog/md/:slug → /md/:slug に変更
    },
  ];
}
```

### 5. ヘッダーナビゲーション更新

`src/components/shared/Header.tsx`：
- `NAVIGATION_LINKS` 配列を更新
- `Blog` を削除、`About` を追加
- アイコンは `IoMdPerson` または `MdOutlinePerson` を検討

### 6. サイト設定の更新（必要に応じて）

`src/config/site.ts`：
- メタデータの `description` を確認
- トップページがブログ一覧になるため、説明文の見直しが必要かもしれない

## 影響範囲

### 変更が必要なファイル

**必須:**
1. `src/app/page.tsx` - ブログ一覧に変更
2. `src/app/about/page.tsx` - 新規作成（自己紹介）
3. `src/app/page/[page]/page.tsx` - 移動（ページネーション）
4. `src/app/[slug]/page.tsx` - 移動（記事詳細）+ メタデータURL更新
5. `src/app/ogp/[slug]/route.tsx` - 移動（OGP画像生成）
6. `src/app/md/[slug]/route.ts` - 移動（マークダウンAPI）
7. `src/components/shared/Header.tsx` - ナビゲーション更新
8. `next.config.mjs` - redirects設定追加 + rewrites設定更新

**削除:**
- `src/app/blog/layout.tsx` - ブログレイアウト（メタデータ定義あり）
- `src/app/blog/page.tsx` - 内容をトップページに移行後削除
- `src/app/blog/` ディレクトリ全体

**確認が必要:**
- 内部リンクを持つコンポーネント（`/blog` への参照）
- メタデータ・OGP設定
- sitemap.xml生成（`/blog` の参照があれば更新）

### SEO・外部リンクへの影響

**重要:** `/blog` へのリダイレクトは必須
- 既存のブックマーク対応
- Google検索インデックス対応
- 外部リンク対応

## 作業手順

### Phase 1: ファイル作成・移動
1. `src/app/about/` ディレクトリ作成
2. 現在の `src/app/page.tsx` → `src/app/about/page.tsx` にコピー
3. `src/app/blog/page/[page]/` → `src/app/page/[page]/` に移動
4. `src/app/blog/[slug]/` → `src/app/[slug]/` に移動
5. `src/app/blog/ogp/[slug]/` → `src/app/ogp/[slug]/` に移動
6. `src/app/blog/md/[slug]/` → `src/app/md/[slug]/` に移動

### Phase 2: 内容更新
7. `src/app/page.tsx` をブログ一覧の内容で上書き（現在の `/blog/page.tsx` の内容）
   - リンク `/blog/page/1` → `/page/1` に変更
8. `src/app/page/[page]/page.tsx` 内のリンク更新（`/blog/` → `/`）
9. `src/app/[slug]/page.tsx` 内のリンク・メタデータ更新
   - メタデータ内の `/blog/${slug}` → `/${slug}`
   - 戻るリンク `/blog` → `/`
10. `src/app/ogp/[slug]/route.tsx` 内の参照更新（必要に応じて）
11. `src/app/md/[slug]/route.ts` 内の参照更新（必要に応じて）

### Phase 3: ナビゲーション更新
12. `src/components/shared/Header.tsx` のナビゲーション更新
   - `Blog` 削除、`About` 追加

### Phase 4: Next.js設定更新
13. `next.config.mjs` にredirects設定を追加
14. `next.config.mjs` のrewrites設定を更新（`/blog/:slug.md` → `/:slug.md`）

### Phase 5: クリーンアップ
15. `src/app/blog/` ディレクトリ削除（すべてのファイルが移動済みであることを確認）

## 重要な注意点

### リンク参照の更新箇所

以下のパターンを検索して更新：
- `/blog` → `/` または `/about`
- `/blog/page/` → `/page/`
- `/blog/[slug]` → `/[slug]`
- `/blog/${slug}` → `/${slug}` （メタデータ内のURL）

**確認済みの変更箇所:**
1. `src/components/shared/Header.tsx:33` - ナビゲーションリンク
2. `src/app/blog/page.tsx:27` - 「すべての投稿を見る」リンク
3. `src/app/blog/[slug]/page.tsx` - 戻るリンク + メタデータ内のURL
   - Line 36: `canonical: absoluteUrl(\`/blog/${post.slug}\`)`
   - Line 42: `url: absoluteUrl(\`/blog/${post.slug}\`)`
   - Line 43: `images: [absoluteUrl(\`/blog/ogp/${post.slug}\`)]`

### コンポーネント内のリンク

検索すべきファイル：
```bash
grep -r "/blog" src/components/
grep -r "/blog" src/app/
grep -r "href.*blog" src/
grep -r "absoluteUrl.*blog" src/
```

### ビルド・型チェック

変更後に以下を実行：
```bash
bun run type-check
bun run lint
bun run build
```

### テスト項目

- `/` でブログ一覧が表示される
- `/about` で自己紹介が表示される
- `/page/1` でページネーションが機能する
- `/[slug]` でブログ記事詳細が表示される
- `/blog` から `/` にリダイレクトされる
- ヘッダーナビゲーションが正しく動作する
- アクティブリンクのハイライトが正しい

## 補足

### layoutファイルの扱い

**重要:** `src/app/blog/layout.tsx` が存在し、以下のメタデータを定義している：
```typescript
title: 'Blog'
description: '技術記事を投稿しています。ページを選択すると記事ページへ移動します。'
```

**対応方針:**
- このlayoutファイルは**削除**する（`/blog` ルートが廃止されるため）
- トップページ（`/`）のメタデータは `src/app/layout.tsx` で定義されている内容を使用
- 必要に応じて `src/app/layout.tsx` のメタデータを調整（ブログ一覧がトップになるため）
- ルートの `src/app/layout.tsx` は基本的に変更不要だが、descriptionの見直しは検討

### メタデータの更新

各ページの `metadata` export を確認：
- トップページ: ブログ一覧としてのメタデータ
- Aboutページ: 自己紹介としてのメタデータ
